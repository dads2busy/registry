@model Questionnaire2.Models.Questionnaire

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Questionnaire</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.QuestionnaireName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.QuestionnaireName)
            @Html.ValidationMessageFor(model => model.QuestionnaireName)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
